states_sources = [
  'TempState.cpp'
]

states_headers = [
  'TempState.hpp'
]

states_deps = [
  engine_dep
]

states_includes = [
  engine_inc
]

states_lib = static_library('computergame.states', states_sources + states_headers, dependencies: states_deps, include_directories: states_includes)
states_dep = declare_dependency(link_with : states_lib, sources: states_sources + states_headers, include_directories: states_includes)
states_inc = include_directories('.')
